@{
  ViewData["Title"] = "Thông tin cá nhân";
}

<head>
  <link rel="stylesheet" href="~/css/changeCourse.css?v=1" />
  <link rel="stylesheet" href="~/css/changedate.css?v=1" />
</head>
@using System;
@using CourseTeachCook.Models;
@using System.Collections.Generic;
@using System.Linq;

  
  <div class="right-body col-lg-10">
    <div class="content-body">
      <h4>Cập nhật ngày khai giảng</h4>
      @using (var system = new CourseTeachCookContext())
      {
        Course course = ViewBag.course;
        <div class="row" id="head">
          
          <form asp-controller="Admin" asp-action="ChangeDateOfOpening" asp-route-id="@course.CourseId"
          method="POST" enctype="multipart/form-data">
            <div id="content">
              <div class="row content" id="email">
                <p class="col-lg-2">Khóa học</p>
                <p class="col-lg-4 detail">@course.CourseName</p>
              </div>
              <div class="row content" id="email">
                <p class="col-lg-2">Giá khóa học</p>
                <input type="date" name="dateOfOpening" class="detail">
              </div>
              @if (ViewBag.result != null)
              {
                @if (ViewBag.result.Equals(true))
                {
                  <div class="row content">
                    <p class="col-lg-2"></p>
                    <button class="result col-lg-3 btn btn-success">Đổi ngày khai giảng thành công</button>
                  </div>
                }
                else
                {
                  <div class="row content">
                    <p class="col-lg-2"></p>
                    <button class="result col-lg-3 btn btn-danger">Đổi ngày khai giảng thất bại</button>
                  </div>
                }
              }
              <div class="submitForm">
                <input type="submit" value="Lưu thông tin">
              </div>

            </div>
          </form>
        </div>

      }


    </div>
  </div>
